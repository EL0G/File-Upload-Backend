# File-Upload-Backend
Built a FastAPI backend for a file upload application with JWT authentication, user registration, SQLite database storage, and REST API endpoints for managing uploaded file metadata.
